# Angular-Deadline-App

#1 The first solution (manual decrement) is more lightweight and avoids repeated API calls.
# check method checkDeadline() in src\app\deadline\deadline.component.ts

 #2 The second solution uses RxJS reactivity, is robust and it uses RxJs operators
 # check method checkDeadline1() in src\app\deadline\deadline.component.ts
